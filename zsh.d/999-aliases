alias ls='ls --color -F'
alias h=helm
alias k=kubectl
alias hf=helmfile
alias jh=/usr/libexec/java_home
alias py='/usr/bin/env python3'
alias kt=kubetail

alias hd='/usr/bin/ssh -t hdprw.sherwin.com bash .bash_profile.menu'
alias hd2='/usr/bin/ssh hdqak.sherwin.com'

ghrc() {
	tag=$1 ; shift 
	gh release create \
		--verify-tag \
		--notes "New Release $tag" \
		--title "Version $tag" \
		$tag
}
